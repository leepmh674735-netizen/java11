<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
    xmlns:beans="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/security
        http://www.springframework.org/schema/security/spring-security.xsd">

    <!-- 1. HTTP 보안 설정 -->
    <http auto-config="true" use-expressions="true">
        <!-- 모든 요청에 대해 권한 확인 (최소한의 접근 제어 추가) -->
        <intercept-url pattern="/**" access="hasRole('ROLE_USER')" />
        
        <form-login default-target-url="/home.html" />
        <remember-me />
        <logout logout-success-url="/home.html" />
    </http>

    <!-- 2. 인증 관리자 설정 -->
    <authentication-manager>
        <authentication-provider>
            <!-- 3. 인메모리 사용자 서비스 (오타 및 구조 수정) -->
            <user-service>
                <!-- 주의: 권한명은 관례상 ROLE_ 을 붙입니다 -->
                <user name="juan" password="{noop}p@ssword" authorities="ROLE_USER, ROLE_ADMIN" />
                <user name="elvira" password="{noop}p@ssword" authorities="ROLE_USER" />
            </user-service>
        </authentication-provider>
    </authentication-manager>

</beans:beans>
