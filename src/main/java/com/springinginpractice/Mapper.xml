<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mapper.OrderMapper">

    <!-- 주문 내역 삽입 -->
    <insert id="insertOrder" parameterType="OrderRequestDTO">
        INSERT INTO orders (product_id, quantity, user_id, order_date)
        VALUES (#{productId}, #{quantity}, #{userId}, NOW())
    </insert>

    <!-- 재고 조회 -->
    <select id="getStockQuantity" resultType="int">
        SELECT stock_quantity 
        FROM products 
        WHERE id = #{productId}
    </select>

    <!-- 재고 차감 (중요: 재고가 요청 수량보다 많을 때만 업데이트) -->
    <update id="updateStock">
        UPDATE products 
        SET stock_quantity = stock_quantity - #{quantity}
        WHERE id = #{productId} 
          AND stock_quantity >= #{quantity}
    </update>

</mapper>